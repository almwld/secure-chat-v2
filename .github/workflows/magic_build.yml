name: Cardia Cloud Magic Build 2026

on:
  push:
    branches: [ main, master ] # سيعمل السحر عند أي تحديث للكود
  workflow_dispatch: # يتيح لك تشغيل البناء يدوياً من موقع GitHub

jobs:
  build_production:
    name: "🚀 Generate Ultimate APK"
    runs-on: ubuntu-latest
    
    steps:
      - name: "📂 Fetching Sources"
        uses: actions/checkout@v4

      - name: "☕ Setting up Java Environment"
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: "⚡ Initializing Flutter Magic"
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          # نستخدم أحدث نسخة مستقرة لعام 2026
          flutter-version: '3.22.0' 

      - name: "📦 Pulling Secret Libraries"
        run: flutter pub get

      - name: "🛠️ Forging the Ultimate APK"
        run: flutter build apk --release --split-per-abi

      - name: "🎁 Delivering the Magic APK"
        uses: actions/upload-artifact@v4
        with:
          name: Cardia-Ultimate-v2026
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 7 # سيبقى الملف متاحاً للتحميل لمدة 7 أيام
